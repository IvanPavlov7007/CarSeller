using TMPro;
using UnityEngine;
using UnityEngine.UI;
using Pixelplacement;
using System;

public class PrimarySelectionCarButtonView : MonoBehaviour
{
    //StateMachine stateMachine;
    //protected override RectTransform childrenContainer => null;

    //CarButtonView[] stateUIs;

    //private void Awake()
    //{
    //    stateMachine = GetComponent<StateMachine>();
    //    stateUIs = GetComponentsInChildren<CarButtonView>(true);
    //}

    //protected override void Bind(PrimarySelectionCarButtonWidgetCreator widget)
    //{
    //    foreach (var stateUI in stateUIs)
    //    {
    //        stateUI.SetUp(widget);
    //    }
    //    stateMachine.ChangeState((int)widget.CurrentSelectionState);
    //}
}